//Alternatively we can define constants in TS as follows. 

// enum Color { RED = 2, GREEN = 100, BLUE };
enum Color { RED, GREEN, BLUE };
// console.log(Color.RED);
// console.log(Color.GREEN);
// console.log(Color.BLUE);
